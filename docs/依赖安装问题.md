# 依赖安装问题解决方案

## 当前状态

系统已基本完成，但遇到以下依赖问题：

1. ✅ **mediapipe** - 已安装但版本API不兼容
2. ⚠️ **rPPG-Toolbox** - 缺少scikit-image（仅RGB模式需要）
3. ✅ **其他依赖** - 全部正常

---

## 解决方案

### 方案A：手动安装mediapipe（推荐）

由于网络问题，请手动下载安装：

1. **下载mediapipe whl文件**：
   - 访问: https://pypi.org/project/mediapipe/#files
   - 下载: `mediapipe-0.10.11-cp310-cp310-win_amd64.whl`
   - 或使用已下载的: `mediapipe-0.10.33-py3-none-win_amd64.whl`

2. **安装**：
   ```bash
   cd D:\guangdian\26-Light\python
   pip install mediapipe-0.10.11-cp310-cp310-win_amd64.whl
   ```

3. **验证**：
   ```bash
   python -c "import mediapipe as mp; print(mp.__version__); print(hasattr(mp, 'solutions'))"
   ```

   预期输出：
   ```
   0.10.11
   True
   ```

---

### 方案B：使用已安装的mediapipe 0.10.33

如果mediapipe 0.10.33确实有`solutions`属性，可以直接使用。

**测试步骤**：

```bash
# 重新安装mediapipe 0.10.33
pip install D:\guangdian\26-Light\python\.cache\mediapipe-0.10.33-py3-none-win_amd64.whl

# 测试
python -c "import mediapipe as mp; print(hasattr(mp, 'solutions'))"
```

如果输出`True`，说明可以使用。

---

### 方案C：禁用行为检测（临时方案）

如果mediapipe无法安装，可以暂时禁用行为检测功能，只使用NIR-rPPG：

**修改main.py**：

在第84行附近，将：
```python
# MediaPipe Face Mesh（行为检测共用）
mp_face = get_face_mesh(...)
```

改为：
```python
# 禁用MediaPipe（临时）
mp_face = None
print("[警告] MediaPipe未启用，行为检测功能禁用")
```

在第143-151行，添加检查：
```python
# --- 行为特征（每帧都跑 MediaPipe） ---
if mp_face is not None:
    rgb_frame = frame[:, :, ::-1]
    mp_result = mp_face.process(rgb_frame)
    landmarks = None
    if mp_result.multi_face_landmarks:
        landmarks = mp_result.multi_face_landmarks[0].landmark

    perclos = eye_det.update(landmarks)
    yawn_rate = yawn_det.update(landmarks)
    head_pitch = head_est.update(landmarks, frame.shape)
else:
    # 使用默认值
    perclos = 0.0
    yawn_rate = 0
    head_pitch = 0.0
```

这样系统仍然可以运行，只是没有PERCLOS、哈欠、头姿检测。

---

## 当前可用功能

即使没有mediapipe，以下功能仍然可用：

✅ **NIR-rPPG心率检测**
- 核心功能完整
- 不依赖mediapipe
- 可以单独运行

✅ **相机采集**
✅ **人脸检测**
✅ **串口通信**
✅ **LED灯带控制**

❌ **行为检测**（需要mediapipe）
- PERCLOS（闭眼检测）
- 哈欠检测
- 头姿检测

---

## 测试NIR-rPPG（不需要mediapipe）

创建简化测试脚本 `test_nir_only.py`：

```python
"""仅测试NIR-rPPG功能（不需要mediapipe）"""

import sys
import os
import time

sys.path.insert(0, os.path.dirname(__file__))

from config import CONFIG
from capture.camera import Camera
from face.detector import FaceDetector
from face.roi import extract_roi
from rppg.signal_buffer import SignalBuffer
from rppg.unsupervised import extract_bvp
from rppg.hr_estimator import estimate_hr

def main():
    print("=" * 60)
    print("NIR-rPPG 心率检测测试（无需mediapipe）")
    print("=" * 60)

    camera = Camera(CONFIG)
    detector = FaceDetector(CONFIG)
    signal_buffer = SignalBuffer(CONFIG["camera_fps"], CONFIG["signal_window_sec"])

    print(f"\n采集信号中...")
    print(f"需要 {CONFIG['signal_window_sec']} 秒")

    frame_count = 0
    while not signal_buffer.is_ready():
        ret, frame = camera.read()
        if not ret:
            continue

        face_box = detector.detect(frame)
        if face_box is None:
            continue

        roi = extract_roi(frame, face_box, CONFIG)
        if roi is None:
            continue

        signal_buffer.append(roi)
        frame_count += 1

        if frame_count % 30 == 0:
            progress = len(signal_buffer) / signal_buffer.max_len * 100
            print(f"  进度: {progress:.0f}%")

    print(f"\n提取BVP信号...")
    roi_frames = signal_buffer.get_frames()
    bvp = extract_bvp(roi_frames, CONFIG["camera_fps"],
                     method=CONFIG["rppg_method"],
                     is_nir=CONFIG["is_nir_camera"])

    print(f"估计心率...")
    hr = estimate_hr(bvp, CONFIG["camera_fps"])

    print(f"\n" + "=" * 60)
    print(f"心率: {hr:.1f} BPM")
    print(f"=" * 60)

    camera.release()

if __name__ == "__main__":
    main()
```

运行：
```bash
cd D:\guangdian\26-Light\python
python test_nir_only.py
```

---

## 下一步操作

### 立即可做（不需要mediapipe）：

```bash
cd D:\guangdian\26-Light\python
python test_nir_only.py
```

### 完整功能（需要mediapipe）：

1. 手动下载并安装mediapipe
2. 或使用方案C禁用行为检测
3. 运行完整系统

---

## 联系方式

如果需要帮助：
1. 检查mediapipe是否已在其他位置安装
2. 尝试使用离线whl文件安装
3. 或暂时使用方案C（禁用行为检测）

**重要**: NIR-rPPG核心功能不受影响，可以正常使用！
